import React from 'react';
import './spinner.scss';

const Spinner = () => (
  <div className="loader__container">
    <div className="loader__spinner">Loading...</div>
  </div>
);

export {
  Spinner,
}
